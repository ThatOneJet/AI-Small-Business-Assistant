# ── Python ──
.env
.env.local
__pycache__/
*.pyc
.streamlit/
*.egg-info/

# ── OS ──
.DS_Store

# ── Node / deps ──
node_modules/

# ── Build output ──
# NOTE: `build/` is NOT ignored — it holds the electron-builder icon resources
# (build/icon.ico/png/svg). Only the generated PyInstaller/electron output below.
dist/
out/
dist-electron/
dist-pyinstaller/
build-pyinstaller/

# ── Desktop runtime data (pywebview / WebView2 cache, cookies, etc.) ──
.webview_data/

# Runtime SQLite DB (regenerated by the backend; never track)
optiflow.db

# Release token (fine-grained PAT, JetCore repo only) — NEVER commit/bundle.
.gh-token
/design/
